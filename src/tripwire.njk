{% extends "layouts/base.njk" %}

{# --- МЕТАДАННЫЕ СТРАНИЦЫ --- #}
{% set title = settings.tripwire_page.title %}
{% set permalink = "/practitioner-offer/index.html" %} {# Устанавливаем красивый URL: your-domain.com/practitioner-offer/ #}

{# --- ПОДКЛЮЧАЕМ УНИКАЛЬНЫЕ СТИЛИ ЭТОЙ СТРАНИЦЫ --- #}
{% block pageStyles %}
  <link rel="stylesheet" href="/css/tripwire.css">
{% endblock %}

{# --- УНИКАЛЬНЫЙ КОНТЕНТ ЭТОЙ СТРАНИЦЫ --- #}
{% block mainContent %}
<div class="tripwire-container-yrslv-ultra" style="font-family: 'Roboto', sans-serif; --primary-color-yrslv: #4A90E2; --secondary-color-yrslv: #16A085; --accent-color-yrslv: #e67e22; --text-color-yrslv: #E0E0E0; --dark-text-color-yrslv: #121212; --card-bg: rgba(30, 30, 46, 0.85); --plan-bg: #1e1e2e; --plan-border: #444466; background: none;">
    <div class="tripwire-box-yrslv-ultra">
        <div class="header-yrslv-ultra">
            <span class="icon-yrslv-ultra">🚀</span>
            <h1>{{ settings.tripwire_page.headline }}</h1>
            <p>{{ settings.tripwire_page.subheadline }}</p>
        </div>

        <div class="features-list-yrslv-ultra">
            {% for feature in settings.tripwire_page.features %}
            <div class="feature-item-yrslv-ultra">
                <span class="icon-feature-yrslv-ultra">{{ feature.icon }}</span>
                <div>
                    <strong>{{ feature.title }}</strong>
                    <p>{{ feature.description }}</p>
                </div>
            </div>
            {% endfor %}
        </div>

        <div class="trust-block-yrslv-ultra">
            <h3><span class="lightbulb-icon">💡</span> {{ settings.tripwire_page.trust_block.headline }}</h3>
            <p>{{ settings.tripwire_page.trust_block.text }}</p>
        </div>

        <div class="path-section-yrslv-ultra">
            <h3>Как это работает? Твой быстрый старт в 3 шага:</h3>
            {% for step in settings.tripwire_page.path_steps %}
            <div class="path-step-yrslv-ultra">
                <div class="step-number-path-yrslv-ultra">{{ loop.index }}</div>
                <div class="step-content-yrslv-ultra">
                    <strong>{{ step.title }}</strong>
                    <p>{{ step.description }}</p>
                </div>
            </div>
            {% endfor %}
        </div>
        
        <div class="trust-block-yrslv-ultra">
            <h3><span class="lightbulb-icon">💡</span> Что значит "Умный Способ Оплаты"?</h3>
            <p>Ты не просто покупаешь систему. Ты приобретаешь продукт через платформу SetHubble, которая автоматически делает тебя партнером. Это дает тебе три ключевых преимущества:</p>
            <div class="smart-payment-explanation-yrslv-ultra">
                <ul class="smart-payment-list-yrslv-ultra">
                    <li><strong>Максимальная Выгода:</strong> Ты получаешь 50% с каждой личной продажи. Это одна из самых щедрых программ на рынке.</li>
                    <li><strong>Настоящий Пассивный Доход:</strong> Ты строишь пассивный доход, получая проценты с продаж твоей команды на 2 уровня в глубину.</li>
                    <li><strong>Честность и Автоматизация:</strong> Все расчеты и выплаты происходят автоматически и прозрачно на платформе SetHubble.</li>
                </ul>
            </div>
        </div>

        <div class="price-wrapper-yrslv-ultra">
            <span class="old-price-yrslv-ultra">{{ settings.tripwire_page.price_old }}</span>
            <div class="price-yrslv-ultra">{{ settings.tripwire_page.price_new }}</div>
            <p>(Единоразовая инвестиция)</p>
        </div>

        <div class="cta-section-yrslv-ultra">
            <a href="{{ settings.integrations.links.payment_practitioner }}" class="cta-button-yrslv-ultra">
                {{ settings.tripwire_page.cta_button_text }}
            </a>
        </div>
    </div>
    
    {# Подключаем тот же скрипт, что и на главной #}
    <script src="/js/footer-script.js" defer></script>
</div>
{% endblock %}
