{% extends "layouts/base.njk" %}

{# --- МЕТАДАННЫЕ --- #}
{% set title = settings.tripwire_page.title %}

{# --- ПОДКЛЮЧАЕМ УНИКАЛЬНЫЕ СТИЛИ ЭТОЙ СТРАНИЦЫ --- #}
{% block pageStyles %}
  <link rel="stylesheet" href="/css/tripwire.css">
  <style>
    /* Дополнительные стили для нового блока, можно потом перенести в tripwire.css */
    .smart-payment-list { list-style: none; padding-left: 0; margin-top: 20px; }
    .smart-payment-list li { position: relative; padding-left: 30px; margin-bottom: 15px; }
    .smart-payment-list li::before { content: '⚡'; color: var(--accent-color); position: absolute; left: 0; font-size: 1.4em; }
  </style>
{% endblock %}

{# --- УНИКАЛЬНЫЙ КОНТЕНТ --- #}
{% block mainContent %}
<div class="page-container">
  
    <div class="content-wrapper">
        <h1 class="main-title">{{ settings.tripwire_page.headline }}</h1>
        <p class="subtitle">{{ settings.tripwire_page.subheadline }}</p>
        
        <ul class="feature-list">
            {% for feature in settings.tripwire_page.features %}
            <li>
                <div class="icon">{{ feature.icon }}</div>
                <div class="text">
                    <strong>{{ feature.title }}</strong>
                    <p>{{ feature.description }}</p>
                </div>
            </li>
            {% endfor %}
        </ul>

        <div class="trust-block">
            <h3>💡 {{ settings.tripwire_page.trust_block.headline }}</h3>
            <p>{{ settings.tripwire_page.trust_block.text }}</p>
        </div>
        
        <h3 class="main-title" style="font-size: 2em; margin-top: 60px;">{{ settings.tripwire_page.path_section_headline }}</h3>
        <ul class="path-steps">
            {% for step in settings.tripwire_page.path_steps %}
            <li>
                <div class="icon">{{ loop.index }}</div>
                <div class="text">
                    <strong>{{ step.title }}</strong>
                    <p>{{ step.description }}</p>
                </div>
            </li>
            {% endfor %}
        </ul>
        
        {# ✅ ИСПРАВЛЕННЫЙ И ДОПОЛНЕННЫЙ БЛОК #}
        <div class="trust-block">
            <h3>💡 {{ settings.smart_payment_block.headline }}</h3>
            <p>{{ settings.smart_payment_block.text }}</p>
            <ul class="smart-payment-list">
                {% for item in settings.smart_payment_block.benefits %}
                    <li>{{ item | safe }}</li>
                {% endfor %}
            </ul>
        </div>
        
        <div class="price-block">
            <div class="old-price">{{ settings.tripwire_page.price_old }}</div>
            <div class="new-price">{{ settings.tripwire_page.price_new }}</div>
            <div class="price-note">(Единоразовая инвестиция)</div>
        </div>
        
        <a href="{{ settings.integrations.links.payment_practitioner }}" class="cta-button cta-button--shaking">
            {{ settings.tripwire_page.cta_button_text }}
        </a>
    </div>
    <script src="/js/footer-script.js" defer></script>
</div>
{% endblock %}
